dataset:
  name: adamson_2016
  expression_path: ../../../working_dir/results/py_comparison_adamson_seed1/all_predictions.json
  adata_path: ../data/gears_pert_data/adamson/perturb_processed.h5ad
  gene_names_path: ../../../working_dir/results/py_comparison_adamson_seed1/gene_names.json
  annotation_path: ../data/annotations/adamson_functional_classes_enriched.tsv
  results_dir: ../results/adamson
  seed: 1
  hardness_bins: [0.33, 0.66]
  logo_cluster_block_size: null
  functional_min_class_size: 3

model:
  type: linear
  gene_embedding: training_data
  pert_embedding: external
  pca_dim: 10
  ridge_penalty: 0.1

tasks:
  - class
  # Add `logo` to run the AnnData-based leave-one-GO-out workflow from this config.

output_root: ../results/adamson

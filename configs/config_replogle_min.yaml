dataset:
  name: replogle_k562_essential
  expression_path: ../../../../working_dir/results/py_comparison_replogle_k562_essential_seed1/all_predictions.json
  adata_path: ../data/gears_pert_data/replogle_k562_essential/perturb_processed.h5ad
  gene_names_path: ../../../../working_dir/results/py_comparison_replogle_k562_essential_seed1/gene_names.json
  annotation_path: data/annotations/replogle_k562_functional_classes_go.tsv
  results_dir: ../results/replogle_min
  seed: 1
  hardness_bins: [0.33, 0.66]
  hardness_method: min  # Use minimum similarity (tail-sensitive) for large dataset
  k_farthest: 10
  logo_cluster_block_size: null
  functional_min_class_size: 3

model:
  type: linear
  gene_embedding: training_data
  pert_embedding: external
  pca_dim: 10
  ridge_penalty: 0.1

tasks:
  - class
  # Add `logo` to run the AnnData-based leave-one-GO-out workflow from this config.

output_root: ../results/replogle_min

